# Define the structure of your API with the GraphQL
# schema definition language (SDL) here.

type CreateEventInput {
	userId: Int!
	payload: AWSJSON!
}

type Mutation {
	createEvent(input: CreateEventInput): String
}

type Query {
	getEvent(testItem: String): String
}

schema {
	query: Query
	mutation: Mutation
}