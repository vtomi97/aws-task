# Define the structure of your API with the GraphQL
# schema definition language (SDL) here.

tpye Mutation {
	createEvent(input: String): String
}

type Query {
	getEvent(id: String): String
}

schema {
	mutation: Mutation
	query: Query
}